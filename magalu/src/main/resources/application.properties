# Database Configuration - MySQL R2DBC
spring.r2dbc.url=r2dbc:mysql://localhost:3306/payment_sim_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.r2dbc.username=payment_sim_user
spring.r2dbc.password=example

# Connection Pool Configuration
spring.r2dbc.pool.enabled=true
spring.r2dbc.pool.initial-size=10
spring.r2dbc.pool.max-size=20
spring.r2dbc.pool.max-idle-time=30m
spring.r2dbc.pool.max-acquire-time=30s
spring.r2dbc.pool.max-create-connection-time=30s

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.validate-on-migrate=true

# Application Configuration
spring.application.name=magalu-checkout
server.port=38080

# Logging Configuration
logging.level.root=INFO
logging.level.com.magalu.checkout=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=when-authorized
management.prometheus.metrics.export.enabled=true

# JWT Configuration
# CHANGE SECRET KEY TO AN ENV VARIABLE
app.jwt.secret=062ed3fb4a6bd706abf5aeb21612af5843dd252dff8a627a3d9c29f1c552d50d9d5a66d1
app.jwt.access-token-expiration=3600